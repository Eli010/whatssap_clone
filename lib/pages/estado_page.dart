import 'package:flutter/material.dart';

class EstadosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Bienvenido Estados Page'),
      ),
    );
  }
}
